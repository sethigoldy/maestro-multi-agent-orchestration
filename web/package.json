{
  "name": "maestro-console",
  "private": true,
  "version": "0.9.0",
  "description": "Maestro web console (React, no build step at runtime)",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
